apiVersion: v1
kind: Service
metadata:
  name: {{ include "ng-chart.fullname" . }}
  labels:
    {{- include "ng-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "ng-chart.selectorLabels" . | nindent 4 }}
