apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ng-chart.fullname" . }}-test-connection"
  labels:
    {{- include "ng-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ng-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
